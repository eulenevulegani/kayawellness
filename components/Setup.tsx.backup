
import React, { useState, useEffect } from 'react';
import { UserProfile } from '../types';
import { ArrowLeftIcon, KayaIcon } from './Icons';

interface SetupProps {
  onComplete: (profile: UserProfile) => void;
}

const ALL_GOALS = [
  { id: 'stress', label: 'Navigate to Serenity', subtitle: 'Manage daily stress' },
  { id: 'sleep', label: 'Journey to Restful Realms', subtitle: 'Improve sleep quality' },
  { id: 'grounded', label: 'Find Your Center', subtitle: 'Stay grounded & present' },
  { id: 'gratitude', label: 'Cultivate Cosmic Gratitude', subtitle: 'Practice gratitude daily' },
  { id: 'anxiety', label: 'Calm the Cosmic Storm', subtitle: 'Manage anxiety' },
  { id: 'focus', label: 'Sharpen Stellar Focus', subtitle: 'Increase concentration' },
  { id: 'resilience', label: 'Strengthen Stellar Core', subtitle: 'Build inner resilience' },
  { id: 'balance', label: 'Harmonize Your Orbit', subtitle: 'Work-life balance' },
  { id: 'self-love', label: 'Radiate Inner Light', subtitle: 'Cultivate self-love' }
];
const CHECK_IN_OPTIONS: ('morning' | 'midday' | 'night' | 'personalized')[] = ['morning', 'midday', 'night', 'personalized'];
// lifestyle options removed to streamline setup flow
const SUPPORT_PREFERENCES = ['Professional Therapy', 'Self-Guided Practice', 'Community Support', 'Group Sessions', 'Not Sure Yet'];
const EXPERIENCE_LEVELS = [
    { id: 'beginner', label: 'New to the Cosmos', description: "I'm just beginning to explore", icon: '🌱' },
    { id: 'intermediate', label: 'Emerging Star', description: "I've practiced mindfulness before", icon: '⭐' },
    { id: 'advanced', label: 'Cosmic Navigator', description: 'I have an established practice', icon: '✨' }
];

const ProgressBar: React.FC<{ current: number, total: number }> = ({ current, total }) => (
    <div className="mb-8">
        <div className="flex justify-between items-center mb-3">
            <span className="text-sm text-white/70">Step {current} of {total}</span>
            <span className="text-sm text-cyan-400 font-medium">{Math.round((current / total) * 100)}% Complete</span>
        </div>
        <div className="w-full bg-white/10 rounded-full h-2 overflow-hidden">
            <div 
                className="h-full bg-gradient-to-r from-cyan-400 to-teal-400 transition-all duration-500 ease-out"
                style={{ width: `${(current / total) * 100}%` }}
            />
        </div>
    </div>
);


const Setup: React.FC<SetupProps> = ({ onComplete }) => {
  // Load saved onboarding progress from localStorage
  const loadSavedProgress = () => {
    try {
      const saved = localStorage.getItem('kaya-onboarding-progress');
      if (saved) {
        return JSON.parse(saved);
      }
    } catch (error) {
      console.error('Failed to load onboarding progress:', error);
    }
    return null;
  };

  // Clear any old saved progress since we simplified the flow
  useEffect(() => {
    localStorage.removeItem('kaya-onboarding-progress');
  }, []);

  const [step, setStep] = useState(1);
  const totalSteps = 2; // Welcome, Goals
  const [goals, setGoals] = useState<string[]>([]);

  const handleGoalToggle = (goalId: string) => {
    const newGoals = goals.includes(goalId)
      ? goals.filter(g => g !== goalId)
      : [...goals, goalId];
    setGoals(newGoals);
  };

  const handleNext = () => setStep(prev => Math.min(prev + 1, totalSteps));
  const handleBack = () => setStep(prev => Math.max(prev - 1, 1));

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    if (step < totalSteps) {
        handleNext();
        return;
    }
    
    // Complete setup with just goals - no authentication needed
    localStorage.removeItem('kaya-onboarding-progress');
    
    onComplete({
      name: 'Welcome',
      goals,
      sessionLength: 'medium',
      checkInTimes: ['midday'],
      lifestyle: 'balanced',
      activeProgramId: null,
      programProgress: 0,
      subscriptionTier: 'stardust',
      lastSessionDate: null,
      supportPreferences: [],
      experienceLevel: 'beginner',
      completedPrograms: [],
      streak: 0,
      xp: 0,
      level: 1,
      notificationPreferences: {
        enabled: false,
        checkInReminders: false,
        streakProtection: false,
        milestoneAlerts: false,
        challengeUpdates: false,
        therapistQA: false,
        calendarSync: false,
        quietHoursStart: '22:00',
        quietHoursEnd: '07:00'
      },
      wakeTime: '07:00',
      sleepTime: '22:00',
      morningWellnessEnabled: false,
      joinedChallenges: [],
      supportGroups: [],
      ageRange: '25-34',
      lifeStage: 'other',
      preferredTime: 'evening',
      sessionLengthPreference: '10-15',
      voicePreference: 'no-preference',
      weeklySessionsUsed: 0,
      lastResetDate: new Date().toISOString()
    });
  };

  const handleCheckInTimeClick = (time: 'morning' | 'midday' | 'night' | 'personalized') => {
    setCheckInTimes([time]);
  };
  
  const isContinueDisabled = () => {
      // Step 1 is welcome screen - can always continue
      if (step === 1) return false;
      // Step 2 requires at least one goal
      if (step === 2 && goals.length === 0) return true;
      return false;
  }

  const renderContent = () => {
    switch(step) {
      case 1:
        return (
          <div key={1} className="animate-fade-in">
            <div className="mb-12 text-center">
                <div className="w-24 h-24 bg-gradient-to-br from-cyan-400/20 to-teal-400/20 rounded-full flex items-center justify-center mx-auto mb-8 animate-gentle-pulse">
                    <KayaIcon className="w-16 h-16 text-cyan-400" />
                </div>
                <h1 className="text-5xl md:text-6xl font-thin text-white tracking-wide mb-4">Welcome to Your Calm Universe</h1>
                <p className="text-white/70 text-lg max-w-2xl mx-auto leading-relaxed">
                    A personalized space for mindfulness, peace, and growth
                </p>
            </div>
          </div>
        );
      case 2:
        return (
          <div key={2} className="animate-fade-in">
            <h2 className="text-4xl md:text-5xl font-light text-white mb-6 text-center">What Stars Are You Reaching For?</h2>
            <p className="text-white/60 mb-8 text-center max-w-2xl mx-auto text-lg">
                Choose the paths that resonate with you
            </p>
            {goals.length > 0 && (
                <div className="mb-6 text-center">
                    <span className="inline-block px-4 py-2 bg-cyan-400/20 text-cyan-300 rounded-full text-sm font-medium">
                        {goals.length} {goals.length === 1 ? 'goal' : 'goals'} selected ✨
                    </span>
                </div>
            )}
            <div className="flex flex-wrap gap-3 justify-center max-w-4xl mx-auto">
              {ALL_GOALS.map((goal) => (
                  <button
                      key={goal.id}
                      type="button"
                      onClick={() => handleGoalToggle(goal.id)}
                      className={`px-6 py-4 rounded-2xl text-base transition transform hover:scale-105 active:scale-95 text-left ${goals.includes(goal.id) ? 'bg-gradient-to-r from-cyan-400 to-teal-400 text-cyan-900 font-semibold shadow-lg shadow-cyan-500/30' : 'bg-white/10 text-white/80 hover:bg-white/20 border border-white/20'}`}
                  >
                      <div className="flex flex-col">
                          <span className="font-semibold text-lg">
                              {goals.includes(goal.id) && '✨ '}
                              {goal.label}
                          </span>
                          <span className={`text-sm mt-1 ${goals.includes(goal.id) ? 'text-cyan-800' : 'text-white/60'}`}>
                              {goal.subtitle}
                          </span>
                      </div>
                  </button>
              ))}
            </div>
          </div>
        );
      default:
        return null;
    }
  };

  return (
    <div className="w-full h-full flex flex-col overflow-hidden">
        {step > 1 && (
            <button type="button" onClick={handleBack} className="fixed top-6 left-6 text-white/60 hover:text-white transition z-50">
                <ArrowLeftIcon className="w-6 h-6" />
            </button>
        )}
        
        <form onSubmit={handleSubmit} className="flex-1 flex flex-col overflow-hidden">
            <div className="flex-1 overflow-y-auto px-4 py-6">
                <div className="w-full max-w-md mx-auto">
                    <ProgressBar current={step} total={totalSteps} />
                    {renderContent()}
                </div>
            </div>

            <div className="flex-shrink-0 p-4">
                <div className="w-full max-w-md mx-auto">
                    <button
                        type="submit"
                        disabled={isContinueDisabled()}
                        className="w-full px-12 py-4 bg-gradient-to-r from-cyan-400 to-teal-400 text-cyan-900 rounded-full font-bold text-lg hover:opacity-90 transform hover:scale-105 transition duration-300 disabled:opacity-30 disabled:scale-100 disabled:cursor-not-allowed shadow-lg shadow-cyan-500/30"
                    >
                        {step === 1 ? 'Continue' : 'Enter Your Universe'}
                    </button>
                </div>
            </div>
        </form>
    </div>
  );
};

export default Setup;
                    <p className="text-white/60 mb-6 text-center max-w-lg mx-auto">
                        Gentle reminders to help you stay connected to your practice. Adjust anytime.
                    </p>
                    <div className="space-y-4 max-w-lg mx-auto">
                        <button 
                            type="button" 
                            onClick={() => setNotificationsEnabled(!notificationsEnabled)}
                            className={`w-full text-left p-5 rounded-xl border-2 transition ${notificationsEnabled ? 'bg-white/10 border-cyan-400' : 'border-white/20 hover:border-white/40'}`}
                        >
                            <div className="flex items-start justify-between">
                                <div className="flex-1">
                                    <p className="font-semibold text-white flex items-center gap-2 mb-1">
                                        🔔 Smart Reminders
                                        {notificationsEnabled && <span className="text-xs bg-cyan-400/20 text-cyan-300 px-2 py-0.5 rounded-full">On</span>}
                                    </p>
                                    <p className="text-sm text-white/70">Gentle nudges for check-ins and streak protection</p>
                                </div>
                                <div className={`ml-3 w-6 h-6 rounded-full border-2 flex items-center justify-center transition ${notificationsEnabled ? 'bg-cyan-400 border-cyan-400' : 'border-white/30'}`}>
                                    {notificationsEnabled && <span className="text-cyan-900 text-sm font-bold">✓</span>}
                                </div>
                            </div>
                        </button>

                        <div className="bg-gradient-to-r from-cyan-500/10 to-teal-500/10 border border-cyan-400/30 rounded-xl p-4 text-center">
                            <p className="text-sm text-cyan-300">
                                ✨ Almost there! Just a couple more details to personalize your experience.
                            </p>
                        </div>
                    </div>
                </div>
            );
        case 8:
            return (
                <div key={8} className="animate-fade-in">
                    <h2 className="text-3xl font-light text-white mb-3 text-center">What shall we call you?</h2>
                    <p className="text-white/60 mb-6 text-center max-w-lg mx-auto">
                        We'd love to know your name and email to personalize your journey and keep your progress safe.
                    </p>
                    <div className="space-y-4 text-left">
                        <div>
                            <label htmlFor="name" className="block text-white/80 mb-2 font-medium">
                                Your Name
                            </label>
                            <input
                                type="text"
                                id="name"
                                value={name}
                                onChange={(e) => setName(e.target.value)}
                                placeholder="Your name"
                                className="w-full p-4 bg-white/5 border border-white/20 rounded-lg text-white placeholder-white/40 focus:outline-none focus:ring-2 focus:ring-cyan-400/50 transition text-lg"
                                required
                                autoFocus
                            />
                            <p className="mt-2 text-sm text-white/50">We'll use this to make your journey personal</p>
                        </div>
                        <div>
                            <label htmlFor="email" className="block text-white/80 mb-2 font-medium">
                                Email Address
                            </label>
                            <input
                                type="email"
                                id="email"
                                value={email}
                                onChange={(e) => setEmail(e.target.value)}
                                placeholder="you@example.com"
                                className="w-full p-4 bg-white/5 border border-white/20 rounded-lg text-white placeholder-white/40 focus:outline-none focus:ring-2 focus:ring-cyan-400/50 transition text-lg"
                                required
                                autoComplete="email"
                            />
                            <p className="mt-2 text-sm text-white/50">To save your progress and access KAYA anywhere</p>
                        </div>

                        <div className="bg-cyan-500/10 border border-cyan-400/30 rounded-xl p-4 text-center">
                            <p className="text-sm text-cyan-300">
                                💡 This helps KAYA personalize your experience throughout the day
                            </p>
                        </div>
                    </div>
                </div>
            );
        case 3:
            return (
                <div key={3} className="animate-fade-in">
                    <h2 className="text-3xl font-light text-white mb-3 text-center">Tell Us About You</h2>
                    <p className="text-white/60 mb-6 text-center max-w-lg mx-auto">
                        This helps us personalize your cosmic journey based on your life stage.
                    </p>
                    <div className="space-y-6 max-w-lg mx-auto">
                        <div>
                            <label className="block text-white/80 mb-3 text-sm font-medium">Age Range</label>
                            <div className="grid grid-cols-2 gap-3">
                                {(['18-24', '25-34', '35-44', '45-54', '55+'] as const).map(range => (
                                    <button
                                        key={range}
                                        type="button"
                                        onClick={() => setAgeRange(range)}
                                        className={`p-4 rounded-xl border-2 transition ${
                                            ageRange === range
                                                ? 'bg-gradient-to-r from-cyan-500/20 to-teal-500/20 border-cyan-400'
                                                : 'border-white/20 bg-white/5 hover:border-white/40'
                                        }`}
                                    >
                                        <span className="text-white font-medium">{range}</span>
                                    </button>
                                ))}
                            </div>
                        </div>
                        <div>
                            <label className="block text-white/80 mb-3 text-sm font-medium">Current Life Stage</label>
                            <div className="grid grid-cols-1 gap-3">
                                {([
                                    { id: 'student', label: 'Student', emoji: '📚' },
                                    { id: 'professional', label: 'Working Professional', emoji: '💼' },
                                    { id: 'parent', label: 'Parent', emoji: '👨‍👩‍👧‍👦' },
                                    { id: 'retired', label: 'Retired', emoji: '🌅' },
                                    { id: 'other', label: 'Other', emoji: '✨' }
                                ] as const).map(stage => (
                                    <button
                                        key={stage.id}
                                        type="button"
                                        onClick={() => setLifeStage(stage.id)}
                                        className={`p-4 rounded-xl border-2 transition text-left flex items-center gap-3 ${
                                            lifeStage === stage.id
                                                ? 'bg-gradient-to-r from-cyan-500/20 to-teal-500/20 border-cyan-400'
                                                : 'border-white/20 bg-white/5 hover:border-white/40'
                                        }`}
                                    >
                                        <span className="text-2xl">{stage.emoji}</span>
                                        <span className="text-white font-medium">{stage.label}</span>
                                    </button>
                                ))}
                            </div>
                        </div>
                    </div>
                </div>
            );
        case 4:
            return (
                <div key={4} className="animate-fade-in">
                    <h2 className="text-3xl font-light text-white mb-3 text-center">Your Wellness Preferences</h2>
                    <p className="text-white/60 mb-6 text-center max-w-lg mx-auto">
                        Help us tailor the perfect experience for you.
                    </p>
                    <div className="space-y-6 max-w-lg mx-auto">
                        <div>
                            <label className="block text-white/80 mb-3 text-sm font-medium">Preferred Practice Time</label>
                            <div className="grid grid-cols-2 gap-3">
                                {(['18-24', '25-34', '35-44', '45-54', '55+'] as const).map(range => (
                                    <button
                                        key={range}
                                        type="button"
                                        onClick={() => setAgeRange(range)}
                                        className={`p-4 rounded-xl border-2 transition ${
                                            ageRange === range
                                                ? 'bg-gradient-to-r from-cyan-500/20 to-teal-500/20 border-cyan-400'
                                                : 'border-white/20 bg-white/5 hover:border-white/40'
                                        }`}
                                    >
                                        <span className="text-white font-medium">{range}</span>
                                    </button>
                                ))}
                            </div>
                        </div>

                        <div>
                            <label className="block text-white/80 mb-3 text-sm font-medium">Life Stage</label>
                            <div className="grid grid-cols-2 gap-3">
                                {([
                                    { id: 'student', label: 'Student' },
                                    { id: 'early-career', label: 'Early Career' },
                                    { id: 'parent', label: 'Parent' },
                                    { id: 'caregiver', label: 'Caregiver' },
                                    { id: 'retired', label: 'Retired' },
                                    { id: 'other', label: 'Other' }
                                ] as const).map(stage => (
                                    <button
                                        key={stage.id}
                                        type="button"
                                        onClick={() => setLifeStage(stage.id)}
                                        className={`p-4 rounded-xl border-2 transition ${
                                            lifeStage === stage.id
                                                ? 'bg-gradient-to-r from-cyan-500/20 to-teal-500/20 border-cyan-400'
                                                : 'border-white/20 bg-white/5 hover:border-white/40'
                                        }`}
                                    >
                                        <span className="text-white font-medium">{stage.label}</span>
                                    </button>
                                ))}
                            </div>
                        </div>
                    </div>
                </div>
            );
        case 5:
            return (
                 <div key={5} className="animate-fade-in">
                    <h2 className="text-3xl font-light text-white mb-3 text-center">Your Check-In Preference</h2>
                    <p className="text-white/60 mb-6 text-center max-w-lg mx-auto">
                        Choose your preferred time for mindful moments. We'll align with your rhythm.
                    </p>
                    <div className="grid grid-cols-1 gap-4 max-w-lg mx-auto">
                        {CHECK_IN_OPTIONS.map((time) => {
                            const isSelected = checkInTimes.length > 0 && checkInTimes[0] === time;
                            const isPremiumOption = time === 'personalized';
                            const timeEmojis = { morning: '🌅', midday: '☀️', night: '🌙', personalized: '✨' };
                            const timeLabels = { morning: 'Morning', midday: 'Midday', night: 'Night', personalized: 'Personalized' };
                            const timeDesc = { 
                                morning: 'Start your day with intention', 
                                midday: 'Midday reset and recharge', 
                                night: 'Evening wind-down sessions',
                                personalized: 'AI learns your optimal times'
                            };
                            
                            return (
                                <button key={time} type="button" 
                                    onClick={() => handleCheckInTimeClick(time)}
                                    disabled={isPremiumOption}
                                    className={`p-5 rounded-xl transition text-left transform hover:scale-[1.02] ${
                                        isSelected
                                        ? 'bg-gradient-to-br from-cyan-400 to-teal-400 text-cyan-900 font-semibold shadow-lg' 
                                        : isPremiumOption
                                        ? 'bg-white/5 text-white/40 border border-white/10 cursor-not-allowed'
                                        : 'bg-white/10 text-white hover:bg-white/20 border border-white/20'
                                    }`}>
                                    <div className="flex items-center gap-4">
                                        <div className="text-3xl">{timeEmojis[time]}</div>
                                        <div className="flex-1">
                                            <div className="flex items-center gap-2 mb-1">
                                                <span className="font-medium text-lg">{timeLabels[time]}</span>
                                                {isPremiumOption && (
                                                    <span className="text-xs bg-yellow-500/20 text-yellow-300 px-2 py-0.5 rounded-full">Premium</span>
                                                )}
                                            </div>
                                            <p className="text-sm opacity-70">{timeDesc[time]}</p>
                        </div>
                                        {isSelected && (
                                            <div className="w-6 h-6 bg-white rounded-full flex items-center justify-center">
                                                <span className="text-cyan-900 text-sm font-bold">✓</span>
                                            </div>
                                        )}
                                    </div>
                                </button>
                            );
                        })}
                    </div>
                </div>
            );
        case 6:
            return (
                 <div key={6} className="animate-fade-in">
                    <h2 className="text-3xl font-light text-white mb-3 text-center">Your Daily Schedule</h2>
                    <p className="text-white/60 mb-6 text-center max-w-lg mx-auto">
                        When does your universe wake and rest? This helps us guide you at the perfect moments.
                    </p>
                    <div className="space-y-6 max-w-md mx-auto">
                        <div>
                            <label className="block text-white/80 mb-3 text-sm font-medium">Preferred Practice Time</label>
                            <div className="grid grid-cols-2 gap-3">
                                {([
                                    { id: 'morning', label: 'Morning', emoji: '🌅' },
                                    { id: 'afternoon', label: 'Afternoon', emoji: '☀️' },
                                    { id: 'evening', label: 'Evening', emoji: '🌆' },
                                    { id: 'late-night', label: 'Late Night', emoji: '🌙' }
                                ] as const).map(time => (
                                    <button
                                        key={time.id}
                                        type="button"
                                        onClick={() => setPreferredTime(time.id)}
                                        className={`p-4 rounded-xl border-2 transition ${
                                            preferredTime === time.id
                                                ? 'bg-gradient-to-r from-cyan-500/20 to-teal-500/20 border-cyan-400'
                                                : 'border-white/20 bg-white/5 hover:border-white/40'
                                        }`}
                                    >
                                        <div className="text-2xl mb-1">{time.emoji}</div>
                                        <span className="text-white text-sm">{time.label}</span>
                                    </button>
                                ))}
                            </div>
                        </div>

                        <div>
                            <label className="block text-white/80 mb-3 text-sm font-medium">Session Length Preference</label>
                            <div className="grid grid-cols-2 gap-3">
                                {([
                                    { id: '3-5', label: '3-5 minutes', desc: 'Quick reset' },
                                    { id: '10-15', label: '10-15 minutes', desc: 'Balanced' },
                                    { id: '20-30', label: '20-30 minutes', desc: 'Deep practice' },
                                    { id: '30+', label: '30+ minutes', desc: 'Immersive' }
                                ] as const).map(length => (
                                    <button
                                        key={length.id}
                                        type="button"
                                        onClick={() => setSessionLengthPreference(length.id)}
                                        className={`p-4 rounded-xl border-2 transition text-left ${
                                            sessionLengthPreference === length.id
                                                ? 'bg-gradient-to-r from-cyan-500/20 to-teal-500/20 border-cyan-400'
                                                : 'border-white/20 bg-white/5 hover:border-white/40'
                                        }`}
                                    >
                                        <div className="text-white font-medium text-sm">{length.label}</div>
                                        <div className="text-white/60 text-xs mt-1">{length.desc}</div>
                                    </button>
                                ))}
                            </div>
                        </div>

                        <div>
                            <label className="block text-white/80 mb-3 text-sm font-medium">Voice Preference (Optional)</label>
                            <div className="grid grid-cols-2 gap-3">
                                {([
                                    { id: 'female', label: 'Female' },
                                    { id: 'male', label: 'Male' },
                                    { id: 'non-binary', label: 'Non-binary' },
                                    { id: 'no-preference', label: 'No Preference' }
                                ] as const).map(voice => (
                                    <button
                                        key={voice.id}
                                        type="button"
                                        onClick={() => setVoicePreference(voice.id)}
                                        className={`p-3 rounded-xl border-2 transition ${
                                            voicePreference === voice.id
                                                ? 'bg-gradient-to-r from-cyan-500/20 to-teal-500/20 border-cyan-400'
                                                : 'border-white/20 bg-white/5 hover:border-white/40'
                                        }`}
                                    >
                                        <span className="text-white text-sm">{voice.label}</span>
                                    </button>
                                ))}
                            </div>
                        </div>
                    </div>
                </div>
            );
        case 6:
            return (
                 <div key={6} className="animate-fade-in">
                    <h2 className="text-3xl font-light text-white mb-3 text-center">Your Daily Schedule</h2>
                    <p className="text-white/60 mb-6 text-center max-w-lg mx-auto">
                        When does your universe wake and rest? This helps us guide you at the perfect moments.
                    </p>
                    <div className="space-y-6 max-w-md mx-auto">
                        <div className="bg-white/5 border border-white/20 rounded-xl p-5">
                            <label className="block text-white font-medium mb-3 flex items-center gap-2">
                                <span className="text-2xl">🌅</span>
                                When does your day begin?
                            </label>
                            <input
                                type="time"
                                value={wakeTime}
                                onChange={(e) => setWakeTime(e.target.value)}
                                className="w-full px-4 py-3 bg-white/10 border border-white/20 rounded-xl text-white text-lg focus:outline-none focus:ring-2 focus:ring-cyan-400 focus:border-transparent"
                            />
                            <p className="text-sm text-white/60 mt-3">
                                We'll suggest morning sessions around this time
                            </p>
                        </div>

                        <div className="bg-white/5 border border-white/20 rounded-xl p-5">
                            <label className="block text-white font-medium mb-3 flex items-center gap-2">
                                <span className="text-2xl">🌙</span>
                                When do you usually go to sleep?
                            </label>
                            <input
                                type="time"
                                value={sleepTime}
                                onChange={(e) => setSleepTime(e.target.value)}
                                className="w-full px-4 py-3 bg-white/10 border border-white/20 rounded-xl text-white text-lg focus:outline-none focus:ring-2 focus:ring-cyan-400 focus:border-transparent"
                            />
                            <p className="text-sm text-white/60 mt-3">
                                Perfect for evening wind-down sessions and sleep stories
                            </p>
                        </div>

                        <div className="bg-cyan-500/10 border border-cyan-400/30 rounded-xl p-4 text-center">
                            <p className="text-sm text-cyan-300">
                                💡 This helps KAYA personalize your experience throughout the day
                            </p>
                        </div>
                    </div>
                </div>
            );
        case 9:
            return (
                <div key={9} className="animate-fade-in">
                    <div className="mb-8 text-center">
                        <div className="w-20 h-20 bg-gradient-to-br from-cyan-400/20 to-teal-400/20 rounded-full flex items-center justify-center mx-auto mb-6 animate-gentle-pulse">
                            <span className="text-4xl">🔐</span>
                        </div>
                        <h2 className="text-3xl md:text-4xl font-thin text-white mb-3">Secure Your Universe</h2>
                        <p className="text-white/60 max-w-md mx-auto">
                            One last step! Create a password to protect your account and access KAYA anywhere.
                        </p>
                    </div>
                    
                    <div className="space-y-4 text-left">
                        <div>
                            <label htmlFor="password" className="block text-white/80 mb-2 font-medium">
                                Create Password
                            </label>
                            <input
                                type="password"
                                id="password"
                                value={password}
                                onChange={(e) => setPassword(e.target.value)}
                                placeholder="At least 6 characters"
                                className="w-full p-4 bg-white/5 border border-white/20 rounded-lg text-white placeholder-white/40 focus:outline-none focus:ring-2 focus:ring-cyan-400/50 transition text-lg"
                                required
                                minLength={6}
                                autoFocus
                                autoComplete="new-password"
                            />
                            <p className="mt-2 text-sm text-white/50">Minimum 6 characters</p>
                        </div>
                        
                        <div className="bg-cyan-500/10 border border-cyan-400/30 rounded-xl p-4 mt-6">
                            <p className="text-sm text-cyan-300 text-center">
                                🎁 <strong>7-day free trial</strong> • No credit card required
                            </p>
                        </div>
                        
                        {isAuthenticating && (
                            <div className="flex items-center justify-center gap-2 text-white/60 py-4">
                                <div className="w-5 h-5 border-2 border-white/20 border-t-white rounded-full animate-spin"></div>
                                <span>Creating your universe...</span>
                            </div>
                        )}
                    </div>
                </div>
            );
    }
  };

  return (
    <div className="w-full h-full flex flex-col overflow-hidden">
        {step > 1 && (
            <button type="button" onClick={handleBack} className="fixed top-6 left-6 text-white/60 hover:text-white transition z-50">
                <ArrowLeftIcon className="w-6 h-6" />
            </button>
        )}
        
        <form onSubmit={handleSubmit} className="flex-1 flex flex-col overflow-hidden">
            <div className="flex-1 overflow-y-auto px-4 py-6">
                <div className="w-full max-w-md mx-auto">
                    <ProgressBar current={step} total={totalSteps} />
                    {renderContent()}
                </div>
            </div>

            <div className="flex-shrink-0 p-4">
                <div className="w-full max-w-md mx-auto">
                    <button
                        type="submit"
                        disabled={isContinueDisabled()}
                        className="w-full px-12 py-4 bg-gradient-to-r from-cyan-400 to-teal-400 text-cyan-900 rounded-full font-bold text-lg hover:opacity-90 transform hover:scale-105 transition duration-300 disabled:opacity-30 disabled:scale-100 disabled:cursor-not-allowed shadow-lg shadow-cyan-500/30"
                    >
                        {step === 1 ? 'Continue' : 'Enter Your Universe'}
                    </button>
                </div>
            </div>
        </form>
    </div>
  );
};

export default Setup;
